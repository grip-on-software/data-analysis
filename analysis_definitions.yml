fields:
    sprint_points_normalization:
        description: "Normalization factor for (log) sprint normalization"
        field: 1
    category_conditions:
        description: "Additional query constraints to group by project or other categories"
        field: ""
    interval_condition:
        description: "Condition to constrain to a certain date interval, with a field parameter"
        field: ""
